import { _decorator, Component, Node } from 'cc';
import { State } from '../../States/State';
const { ccclass, property } = _decorator;

@ccclass('RunState')
export class RunState extends State {
    // Пустой класс, как и в оригинальном коде
}
